apiVersion: v1
kind: Service
metadata:
  name: signal-cli-rest-api
  namespace: signal-cli-rest-api
  labels:
    app: signal-cli-rest-api
spec:
  type: ClusterIP
  selector:
    app: signal-cli-rest-api
  ports:
    - port: 8080
      targetPort: 8080
