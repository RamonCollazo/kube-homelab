---
# RuntimeClass for NVIDIA GPU workloads.
# Maps to the 'nvidia' containerd handler provided by
# siderolabs/nvidia-container-toolkit-lts Talos extension.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
