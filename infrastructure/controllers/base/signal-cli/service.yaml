apiVersion: v1
kind: Service
metadata:
  name: signal-cli
  namespace: signal-cli
  labels:
    app: signal-cli
spec:
  type: ClusterIP
  selector:
    app: signal-cli
  ports:
    - port: 8080
      targetPort: 8080
