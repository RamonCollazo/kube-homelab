apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kagent
resources:
  - ../../base/kagent/
  - ./database.yaml
  - ./secrets.yaml

configurations:
  - kustomizeconfig.yaml

configMapGenerator:
  - name: kagent-values
    files:
      - values.yaml=values.yaml
