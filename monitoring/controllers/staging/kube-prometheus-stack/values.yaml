grafana:
  envValueFrom:
    GF_AUTH_GENERIC_OAUTH_CLIENT_ID:
      secretKeyRef:
        name: grafana-oidc
        key: client-id
    GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET:
      secretKeyRef:
        name: grafana-oidc
        key: client-secret
  grafana.ini:
    server:
      root_url: https://grafana.raymondcollazo.com
    auth:
      signout_redirect_url: "https://auth.raymondcollazo.com/application/o/grafana/end-session/"
      oauth_auto_login: true
    auth.generic_oauth:
      enabled: true
      name: Authentik
      allow_sign_up: true
      scopes: openid profile email
      auth_url: https://auth.raymondcollazo.com/application/o/grafana/authorize/
      token_url: https://auth.raymondcollazo.com/application/o/grafana/token/
      api_url: https://auth.raymondcollazo.com/application/o/userinfo/
      role_attribute_path: "contains(groups, 'grafana Admins') && 'Admin' || contains(groups, 'grafana Editors') && 'Editor' || 'Viewer'"
