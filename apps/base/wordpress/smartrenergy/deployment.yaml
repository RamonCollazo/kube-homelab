---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress-smartrenergy
  namespace: wordpress-smartrenergy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wordpress-smartrenergy
  template:
    metadata:
      labels:
        app: wordpress-smartrenergy
    spec:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 33
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: wordpress
          image: wordpress:6.9.1
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN", "SETGID", "SETUID", "NET_BIND_SERVICE"]
          volumeMounts:
            - name: wordpress-smartrenergy-storage
              mountPath: /var/www/html
          envFrom:
            - configMapRef:
                name: wordpress-smartrenergy-env
            - secretRef:
                name: wordpress-smartrenergy-secret
      volumes:
        - name: wordpress-smartrenergy-storage
          persistentVolumeClaim:
            claimName: wordpress-smartrenergy-pvc
